create this starter project with `create-react-app` using `typescript` template

also add *scss* support, *bootstrap*, *react-bootstrap*

add my prefered editor's `.editorconfig` file

first push to github
